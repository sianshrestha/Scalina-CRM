package com.scalina.crm.model.enums;

public enum Role {
    ADMIN,   // Full access to dashboard, invoices, expenses, project creation
    MEMBER   // Limited access: Can only view and update assigned tasks
}